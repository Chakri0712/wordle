services:
  - type: web
    name: wordle-multiplayer
    runtime: node
    # Minimal build command for backend only
    buildCommand: npm install
    # Serve everything from the Express server (it serves client/dist in production)
    startCommand: node server/index.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
